article.flex
  .narrow
    != post.header
    p.post-date.inline= post.date.display
    //- if post.metadata.isLinkHeader
    //-   .link-icon.inline
    //-     include ../../public/images/link.svg
  .wide
    != post.body